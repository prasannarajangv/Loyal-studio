# loyalstudiowebsite
Github page to host all compnents and the codes and images.

Quick start

- Install dependencies: `npm install`
- Run dev server: `npm run dev`

Note: This project now uses `react-router-dom` for page routing.
